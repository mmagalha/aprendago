package main

import	(
	"fmt"
)

var x int
var y string
var z bool

func main()  {
	fmt.Println(x,"\n",y,"\n",z)
	fmt.Println("os valores atribuidos por padrão pelo compilador são chamados de zero e cada tipo tem um valor diferente")
}